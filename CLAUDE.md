# CLAUDE.md

This repository uses `AGENTS.md` as the single source of truth for local agent
instructions. Read `AGENTS.md` before building, flashing, or editing benchmark
tooling.

Important override: WSL firmware builds must use the repository-level
`./make.sh`; do not use the older manual `cd EPII_CM55M_APP_S && make clean &&
make` workflow.
